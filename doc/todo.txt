ToDo
====

フォームバリデータ

テンプレート内で、フォームに入れられる値の範囲や最大長などを指定します。

メール送信機能

日本語環境で、HTMLメールや添付ファイルつきメールを簡単に送れる関数を実装します。 Windows環境向けに、自分でSMTPをしゃべれるようにします。

Djangoのタグ・フィルタをすべて実装する

MuMuは、DjangoというすばらしいWebフレームワークに強く影響を受けています。Djangoのテンプレートエンジンで対応しているタグ・フィルタの一覧のうち実装していないものを実装していきます。要望をもらえると、そこを優先して実装していきます。

未実装のタグ: ifchanged(実装予定), load, regroup, ssi, url, with(実装予定)
未実装機能があるタグ: extends, include, for(key/value), cycle, endblock(with blockname)
未実装のフィルタ: ↓
フィルタ名  実装予定
dictsort  ○あとで
dictsortreversed  ○あとで
phone2numeric
pluralize
slugify
timesince
timeuntil
truncatewords_html
unordered_list  ○あとで
キャッシュ機能

以下の２つのキャッシュを実装する予定です。

パース済みのテンプレートファイルのキャッシュ（実装済みだが、memcachedにもためられるようにする）
テンプレートに値をあてはめたもののキャッシュ
親切なエラー機能

親切でキレイなエラー画面を出力できるようにします。 本当は、ちょっとだけ実装しているのですが、優先順位が後回しになっています。

高速化

血眼になって高速化を行います。

文字コードフィルタ

エラー表示を日本語で出すためにも、テンプレートの文字コードを変換するフィルタなんかを考えています。

バグ修正

見つかったバグをじゃんじゃん修正します。

モリタポ決済機能

つけます。

pecl化

します。pecl脳だからです。
